# Metadata for the MODS menu section header: "<name> By <author> (v<version>)".
name: UI Fixes
author: Trihardest
version: 1.0.0
description: Fixes vanilla UI alignment, e.g. the co-op ASSIGN INPUT header drifting on ultrawide aspect ratios.
