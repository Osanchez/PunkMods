# Build output
**/bin/
**/obj/

# Packaged distribution zips (regenerated by build-package.ps1)
dist/

# Staging folder used while packaging
dist/PUNK-Mods/

# CI reference-DLL bundle (proprietary — never commit; lives in the private 'refs' release asset)
dist-refs/
punk-refs.zip
gamedir/

# Editor / user files
.vs/
*.user
*.suo
